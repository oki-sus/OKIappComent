
@* 自動生成されたものです *@
@* 画面が呼び出された際、どの画面よりも先に最優先で実行されます。 *@

@{
    @* アプリ内のすべての画面に対して、共通の枠組み（外枠のテンプレート）として「_Layout.cshtml」を自動で適用する設定 *@
    @* これがあるおかげで、各画面（IndexやCreateなど）のコード内に毎回同じヘッダーやサイドバーのコードを書かなくても、自動で共通のデザイン枠にはめ込まれて表示されます。 *@
    Layout = "/Views/Shared/_Layout.cshtml";
}
